<!--
Пометки «Проверка методики», которые сервер дописывает в rationale совета, когда
модель нарушила жёсткую границу и после репромпта — снова. Это текст для атлета
в приложении, не промпт: модель его не видит, отсюда читает plan_validator.resolve.
Сами границы и строки нарушений для репромпта — prompts/plan_rules.md; имена
фрагментов те же, что у правил в plan_validator.RULES.

У правила, которое сервер умеет чинить, не выдумывая чисел (возвратные веса,
размер сессии), здесь два фрагмента: «имя_change» — одна правка, строкой, она же
уходит в трассу отладки Coach MCP; «имя_fixed» — заголовок пометки про все правки
разом. Покрытие групп чинить нельзя (упражнение не выдумать), у него фрагментов
здесь нет: незакрытое нарушение попадает в «unresolved».
-->

## fixed
**Проверка методики:** {{what}}: {{changes}}.

## unresolved
**Проверка методики:** сервер не смог согласовать с моделью: {{violations}} — учти при выполнении.

## return_ceiling_change
{{exercise}}: {{weight}} → {{allowed}} кг (доперерывный рабочий)

## return_ceiling_fixed
возвратные веса ограничены доперерывными рабочими

## session_cap_change
{{exercise}} −{{count}}

## session_cap_fixed
сессия сокращена до {{cap}} рабочих подходов (потолок фазы)
