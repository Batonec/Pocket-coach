<!--
Пометки «Проверка методики», которые сервер дописывает в rationale совета, когда
модель нарушила жёсткую границу и после репромпта — снова. Это текст для атлета
в приложении, не промпт: модель его не видит. Сами правила — prompts/plan_rules.md;
имена здесь те же, что у блоков там.

У правила со строкой «починить:» два фрагмента: «имя_change» — одна правка,
строкой (она же уходит в трассу отладки Coach MCP), «имя_fixed» — заголовок
пометки про все правки разом. Слоты {{имя}} — имена области правила (у процедуры
— её собственные). «fixed» и «unresolved» рендерит сам движок: первая собирает
заголовок и правки правила, вторая перечисляет то, что починить не вышло, —
например покрытие групп, у которого «починить:» нет, потому что упражнение не
выдумать.
-->

## fixed
**Проверка методики:** {{что}}: {{правки}}.

## unresolved
**Проверка методики:** сервер не смог согласовать с моделью: {{нарушения}} — учти при выполнении.

## return_ceiling_change
{{упражнение}}: {{вес}} → {{допустимо}} кг (доперерывный рабочий)

## return_ceiling_fixed
возвратные веса ограничены доперерывными рабочими

## session_cap_change
{{упражнение}} −{{снято}}

## session_cap_fixed
сессия сокращена до {{потолок}} рабочих подходов (потолок фазы)
