"""Скрипты systemd-таймеров: авто-свежесть совета, недельный отчёт, бэкап базы.

Каждый запускается как обычный скрипт (``python3 backend/infra/jobs/<имя>.py``)
и сам кладёт корень backend в ``sys.path``, чтобы импортировать пакет trainer.
Юниты и таймеры — рядом, в ``infra/deploy/``.
"""
