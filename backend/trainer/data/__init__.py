"""Как данные приходят и уходят: SQLite, файлы рядом с базой, шаблоны, HTTP к Claude.

Ничего не решает: правила лежат в ``trainer.domain`` и вызываются отсюда,
когда хранилищу нужно решение (например, что записать при ретрае).
"""
