<!--
Жёсткие границы плана — единственное, что сервер проверяет в ответе модели
поверх JSON-схемы. Этот файл — источник их формулировок: предложение правила
уходит в системный промпт блоком «ЖЁСТКИЕ ГРАНИЦЫ (проверяет сервер)», строка
нарушения — в репромпт, когда план правило нарушил. Модель читает ровно то, что
сервер проверяет, потому что оба текста берутся отсюда. Слоты {{name}}
подставляет coach_prompts.

Правило в коде — запись в plan_validator.RULES с тем же именем: у каждой пара
фрагментов здесь, «имя» (предложение для системного промпта) и «имя_violation»
(строка для репромпта). Тест держит, что имена здесь и в RULES совпадают один к
одному: правило без текста и текст без правила оба падают. Новое правило — это
пара фрагментов здесь, запись в RULES, пометки в resources/plan_notes.md, если
сервер умеет его чинить, и абзац в BUSINESS_LOGIC.md.

catalog_only — не правило валидатора, а свойство JSON-схемы (enum id) и
санитизации ответа: в списке для модели оно стоит первым, чтобы список был
полным. Порядок остальных пунктов — порядок RULES.
-->

## catalog_only
в exercises[] — только упражнения каталога (id из схемы)

## coverage
крупная группа (грудь, спина, квадрицепс/ягодичные) или бицепс бедра, у которых 10+ дней ноль эффективных сетов, должна получить хотя бы 1–2 подхода

## coverage_violation
группа «{{group}}» больше {{days}} дней без единого эффективного подхода и отсутствует в плане — добавь хотя бы 1–2 подхода

## return_ceiling
на возврате после перерыва (≥14 дней) вес любого движения — не выше доперерывного рабочего

## return_ceiling_violation
{{exercise}}: {{what}} {{weight}} кг тяжелее доперерывного рабочего ({{allowed}}) — возвратная сессия не место для прибавки

## session_cap
рабочих подходов в exercises[] — не больше верхней границы коридора сессии текущей фазы (число в КОНТЕКСТЕ); нижняя граница не проверяется — короткая сессия может быть решением

## session_cap_violation
в плане {{total}} рабочих подходов при потолке сессии {{cap}} для этой фазы — сократи до {{cap}}, начиная с изоляции

## reprompt
Твой план нарушает жёсткие границы:
{{violations}}
Исправь ТОЛЬКО эти нарушения, сохрани остальную логику и верни полный план заново в той же JSON-схеме.
